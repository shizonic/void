#!/usr/bin/env sh
# void-update-dots

dir=$(dirname $(realpath -P "$0"))

rsync -r -L ~/app/cnf/ "${dir}../dot" || true # ignore wrong symbolic links

git add "${dir}/../dot"
git commit -m "Updating on $(date +'%x@%X')"
